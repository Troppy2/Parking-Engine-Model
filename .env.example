# Must match os.environ["TOMTOM_API_KEY"] in context/traffic.py exactly.
# A typo here doesn't crash — the adapter fails soft and silently serves
# clear-road defaults forever, which is much harder to notice.
TOMTOM_API_KEY=
REDIS_URL=redis://localhost:6379/0
